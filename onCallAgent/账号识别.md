很好，这些信息已经够把“账号识别智能体”从“泛泛的 Agent 项目”收束成一段**更有业务张力、也更容易被追问**的经历了。

先给你一个核心判断：

## 这块真正该强化的，不是“我做了 RAG”

而是：

**我把一个开放式、误判高、边界复杂的人群账号识别任务，改造成了一个“规则可检索、流程可约束、结果可复核”的识别系统。**

这个主线一出来，面试官就更容易追问，因为它同时有：

- 业务难度
    
- 技术取舍
    
- 工程实现
    
- 指标改善
    

另外，你给的指标定义也帮了很大忙，但我要先纠正一个非常关键的口径：

## 你现在这条结果，不能再写“有效抓取比例从 50% 提升到 80%”

更准确的说法应该是：

**结果集命中率 / 识别准确率从 50% 提升到 80%**

因为按你给的例子：

- 优化前：输出 100 个，人工核验 50 个真符合，precision = 50%
    
- 优化后：输出 50 个，人工核验 40 个真符合，precision = 80%
    

这说明你提升的是**结果集精度**，不是召回率，也不是整体抓取能力。  
这点一定要改，不然大厂面试官很容易顺手问穿。

---

# 一、基于你补充的信息，这段经历最真实、也最强的主故事

我先帮你把真实主线整理出来：

## 业务问题

甲方希望从公开社交平台账号中识别出符合某类特定职业特征的目标用户。  
任务输入可能是：

- 已有账号 ID 列表，要求筛选是否符合目标职业
    
- 仅给定职业要求，由系统自动检索并识别候选账号
    

这个任务的难点在于：

- 职业判断不是简单关键词匹配
    
- 很多用户不会直接写明职业，需要结合 profile 和近期动态综合判断
    
- 边界情况多，纯规则难覆盖
    
- 纯大模型自由推理误判高、可控性差
    

## 你的核心方案

你不是直接让大模型“猜这个人是不是该职业”，而是做了三件关键事：

1. **把甲方需求和边界条件整理成规则知识库**
    
2. **让 Agent 先检索相关规则，再结合账号 profile 和动态做判断**
    
3. **把结果保留在可人工复核的结果集里，而不是黑盒式输出**
    

这个故事比“我做了 RAG + ReAct”强很多，因为它是一个明确的系统设计问题。

---

# 二、这块最值得写进简历的，不是“炫技”，而是这 4 个卖点

## 卖点 1：这是一个开放式识别任务，不是普通问答

这能解释为什么需要 Agent，而不是一个分类接口。

## 卖点 2：你用 RAG 承载的是“规则约束”

不是通用问答知识库，而是**判断标准和边界条件的动态检索**。

## 卖点 3：你做的是“多步识别流程”

不是一句 prompt 出答案，而是：

- 任务解析
    
- 规则检索
    
- 候选信息获取
    
- 特征判断
    
- 结果输出/复核
    

## 卖点 4：你有可解释的指标改善

而且这个改善来自：

- 减少自由推理
    
- 强化规则约束
    
- 优化结果过滤
    

---

# 三、先指出你当前口述里还需要修正的几个点

这一步很重要，不然简历写得再漂亮，面试里也会出问题。

## 1. “Agent 根据职业自己去分析检索和爬取”这句话不要写太满

因为这句很容易被问：

- 具体怎么检索？
    
- 用什么平台搜索？
    
- 怎么拿到候选账号？
    
- 爬取是你自己实现的，还是调现有工具？
    
- 受平台限制怎么办？
    

如果你没有特别成熟的实现细节，这句不建议写成“自己去检索和爬取”。  
更稳的口径是：

**“根据职业特征生成检索线索，并调用现有查询/抓取能力获取候选账号信息。”**

这样既保留自动化流程，又不会把自己逼到“你到底写了多少爬虫”的角落里。

---

## 2. “规则组织成 markdown 文档”不能直接这么写

因为这会显得太浅。

你应该把它讲成：

**“将甲方需求、职业特征判定标准及边界 case 结构化整理为规则文档，并作为可检索规则库供识别流程动态调用。”**

区别很大：

- “markdown 文档”是文件格式
    
- “结构化规则库”是系统设计
    

面试官关心的是后者。

---

## 3. 你现在还缺一个“不是纯 RAG”的深点

你自己也感觉到了，这很对。  
如果你只讲 RAG，面试官会觉得这是标准套路。  
所以你必须补出至少 2 个更像深度的点。

我建议你后面主打这两个：

### 深点 A：规则如何约束模型判断，而不是只做检索增强

也就是：

- 规则不是背景知识
    
- 而是判断标准
    
- 模型不是自由发挥，而是在规则约束下做归因判断
    

这个比“做了知识库”更高级。

### 深点 B：如何把职业识别拆成多步流程

也就是：

- 先找候选
    
- 再看 profile
    
- 再看近期动态
    
- 再结合边界规则判断
    
- 最后输出可复核结果
    

这能把你从“会接框架”变成“会设计任务流”。

---

# 四、给你一版适合大厂简历的强化描述框架

下面这版我已经尽量按你真实信息来写，不额外虚构关键实现。

## 版本 1：稳妥版，适合大多数简历

**目标账号识别智能体研发**

- 面向公开社交平台账号识别场景，负责目标账号识别智能体的后端研发；针对**纯规则覆盖不足、纯大模型推理误判较高**的问题，设计基于 **RAG + Agent 工作流** 的自动化识别方案。
    
- 将甲方需求、职业判定标准及边界 case 结构化整理为可检索规则库，在识别过程中由 Agent 先召回相关规则，再结合候选账号的 profile 与近期动态完成筛选和判断，降低模型自由推理带来的误识别。
    
- 设计并实现从**任务解析、规则检索、候选信息获取、特征判断到结果输出**的识别链路，支持输入已有账号列表进行筛选，或根据职业要求自动生成检索线索并获取候选账号。
    
- 通过规则约束与结果过滤优化识别质量，将结果集人工核验命中率由 **50% 提升至 80%**，并输出可人工复核的结果集与统计报告。
    

这版的优点：

- 可信
    
- 有追问点
    
- 没有把自己说成“全栈爬虫 + 全自动识别平台”
    

---

## 版本 2：更强一点，适合你想突出“设计感”

- 负责目标账号识别 Agent 的核心流程设计与后端实现，面向“仅给定职业要求或候选账号列表”的识别任务，构建基于 **规则检索 + 工具获取 + 受约束判断** 的自动化识别流程。
    
- 将甲方需求沟通结果、职业判定标准及边界情况沉淀为结构化规则库，利用 RAG 在执行过程中动态召回相关规则，替代直接依赖 Prompt 硬编码规则或 LLM 自由判断的方式，提高识别过程的可控性与可解释性。
    
- 将识别任务拆解为**线索生成 / 候选获取 / profile 与动态分析 / 规则匹配 / 结果汇总**等步骤，输出结果集与统计报告，并保留人工复核依据。
    
- 优化后结果集人工核验命中率由 **50% 提升至 80%**。
    

这版更像“你对方案有判断”，但前提是你面试时能接住“受约束判断”和“线索生成”的追问。

---

# 五、我建议你后面面试时把这块固定讲成这个结构

下面这段你几乎可以当口述模板。

## 1 分钟版本

> 我做的不是普通问答型 Agent，而是一个目标账号识别任务。甲方有两类输入，一类是给已有社交平台账号列表，要求筛出符合某类职业特征的账号；另一类是只给职业要求，让系统自动寻找候选账号。  
> 这个任务的难点在于，很多账号不会直接标注职业，需要结合 profile 和近期动态综合判断，而且边界 case 比较多。纯规则方案覆盖不足，纯大模型自由推理又容易误判。  
> 所以我做的核心是把甲方需求和判定边界沉淀成可检索规则库，在执行过程中先检索相关规则，再结合候选账号信息做受约束判断，而不是让模型直接猜。  
> 这样最终把结果集人工核验命中率从 50% 提升到了 80%。

这段的好处是：

- 先讲业务
    
- 再讲矛盾
    
- 再讲方案
    
- 最后讲结果
    

这是最容易引发高质量追问的结构。

---

# 六、你后面最值得准备的 3 个“深挖点”

你说现在只会讲 RAG，不够深。确实。  
所以我帮你把可讲深的点换成下面这三个。

---

## 深挖点 1：你这里的 RAG 不是“知识补充”，而是“规则约束”

这个一定要作为你的核心表达。

你可以这样说：

> 我们这里 RAG 的作用不是一般问答里的补知识，而是把职业识别规则和边界条件从静态文档变成执行过程中动态可调用的判断依据。  
> 模型只负责理解任务和组织流程，真正的判断标准来自召回的本地规则，这样能明显降低自由推理造成的误判。

这句话一出来，面试官会觉得你不是在背标准答案。

---

## 深挖点 2：任务流拆解，而不是单步生成

你要强化“Agent 为什么有必要”。

你可以这样讲：

> 因为这个任务不是一句话判断，而是要先确定识别目标，再获取候选账号信息，然后结合 profile 和近期动态做分析，最后再根据规则输出结果，所以本质上是一个多步任务流。  
> 我把模型、规则和候选信息获取分开，让模型负责流程决策，让规则负责约束标准，让工具负责补充信息。

这就是工程设计感。

---

## 深挖点 3：结果集精度提升到底来自哪里

你得准备一个因果链，而不只是数字。

建议这样讲：

> 之前命中率低，主要是因为模型会根据局部特征过度推断，或者忽略一些边界条件。  
> 引入规则检索后，模型在判断前会先拿到当前任务相关的职业特征和边界定义，再结合 profile 和动态进行比对，所以明显减少了“看起来像、但其实不符合”的误判。

这就是“指标和方案之间的因果解释”。

---

# 七、还有两个点，我建议你在面试里主动补一句，会显得成熟很多

## 1. 结果可复核

你最好主动加一句：

> 最后输出的不只是结论，还会保留判断依据，方便人工复核。

因为这能体现你知道这类任务不能完全黑盒自动化。

---

## 2. 仍然有边界

你再加一句：

> 对于信息量很少、规则覆盖不足或者边界模糊的 case，仍然会保留人工复核。

这一句会显得你更真实，也更稳。

---

# 八、你现在这块还差一个信息，我再问你最后一个关键问题

这个问题补上后，我就能把它改成更像最终版的简历 bullets：

**你所谓“规则库里的边界 case”，能不能举 2~3 类抽象层面的例子？**  
不用泄密，不用说具体职业。只要说抽象类型，比如：

- profile 明示但动态不一致
    
- 动态中存在疑似职业特征但证据不足
    
- 曾任/现任难区分
    
- 个人身份与所在机构强相关但不能直接等价
    
- 多个候选账号特征相似，需要进一步过滤
    

你只要给我这种抽象类型，我就能把“规则约束”这部分再写得明显更有深度。